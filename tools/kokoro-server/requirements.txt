# Core TTS engine (fast ONNX-based TTS for Python >= 3.10)
kokoro-onnx>=0.1.0
onnxruntime>=1.17.0
espeak-ng-loader>=0.1.0  

# Audio I/O
soundfile>=0.12.0
numpy>=1.24.0

# Fast, modern Python HTTP server (Rust-based, no Node.js needed)
uv[standard]>=0.29.0
python-multipart>=0.0.9

# Optional: faster ONNX runtime for Apple Silicon (M1/M2/M3/M4)
# onnxruntime-silicon>=1.17.0 ; platform_system=="Darwin" and platform_machine=="arm64"
