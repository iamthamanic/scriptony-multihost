/**
 * VETILALORAPP — hierarchical structure ripple in VideoEditorTimeline.
 * Location: src/lib/vetilalorapp-feature.ts
 */

/** Film structure trim uses timeline-tree + ripple-engine (book shots still use legacy). */
export const USE_HIERARCHICAL_STRUCTURE_RIPPLE = true;
