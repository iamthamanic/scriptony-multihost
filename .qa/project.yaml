# Project config for verify-ui and agent pipeline
# Generated by @project-setup audit — adjust navigation as routes grow

appRoot: .

devCommand: npm run dev:desktop
devUrl: http://localhost:3000

checksCommand: npm run checks
checksSnippet: 'CHECK_MODE=snippet SHIM_CHECKS_ARGS="--frontend" SHIM_CHANGED_FILES="<paths>" npm run checks'

e2eCommand: npm run test:e2e

styleguide: docs/UI_STYLEGUIDE.md
designDir: .qa/design
acceptanceDir: .qa/acceptance
intakeDir: .qa/intake
evidenceDir: .qa/evidence

locale: de

viewports:
  - name: desktop-tauri
    width: 1280
    height: 800
  - name: desktop-wide
    width: 1440
    height: 900

navigation:
  - label: Projekte
    path: /projects
  - label: Einstellungen
    path: /settings
