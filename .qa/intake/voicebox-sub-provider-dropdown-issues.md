# Issue draft: voicebox-sub-provider-dropdown (DRAFT)

## Slice 1 — Sub-provider dropdown (P0)

**featureSlug:** `voicebox-sub-provider-dropdown`

Replace 3-provider UI with Option A: Eigene Stimmen + each Voicebox preset engine `(via Voicebox)` + ElevenLabs. Single preset catalog per provider switch. Clone CTA only under Eigene Stimmen.

**Depends on:** —

---

User approval for `gh issue create` not requested; implemented inline per user "A go /implement".
